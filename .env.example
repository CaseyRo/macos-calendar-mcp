# macOS Calendar MCP Server Configuration
# 
# Copy this file to .env and customize as needed:
#   cp .env.example .env
#
# The .env file is git-ignored to prevent committing sensitive configuration.
# Environment variables will override values in this file if set.

# Transport mode
# Options: 'stdio' (default) or 'http'
# - stdio: Standard input/output transport (local process communication)
# - http: HTTP/SSE transport (network accessible)
MCP_TRANSPORT=stdio

# HTTP Server Configuration
# These options are only used when MCP_TRANSPORT=http

# Host to bind the HTTP server to
# Default: 0.0.0.0 (all network interfaces)
# Use 127.0.0.1 for localhost only, or 0.0.0.0 for network access
MCP_HTTP_HOST=0.0.0.0

# Port number for HTTP server
# Default: 3000
# Change this if port 3000 is already in use
MCP_HTTP_PORT=3000
